# Build output
build/
.gradle/

# Local secrets — never commit these
app/google-services.json
local.properties

# IDE
.idea/
*.iml

# Keystore secrets — never commit
keystore.properties
*.jks
*.keystore
